% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{perplexica_search_many}
\alias{perplexica_search_many}
\title{Batch search over a vector of queries}
\usage{
perplexica_search_many(
  queries,
  delay = 2,
  checkpoint_every = 25L,
  checkpoint_file = NULL,
  resume_from = NULL,
  max_retries = 2L,
  mode = "speed",
  base_url = perplexica_get_url(),
  verbose = FALSE
)
}
\arguments{
\item{queries}{A character vector of search queries.}

\item{delay}{Seconds to sleep between requests. Default \code{2}.}

\item{checkpoint_every}{Write a checkpoint file every this many rows.
Set to \code{Inf} to disable.}

\item{checkpoint_file}{Path to the CSV checkpoint file. Required when
\code{checkpoint_every} is finite.}

\item{resume_from}{Integer row to resume from. Rows before this index are
read from \code{checkpoint_file}.}

\item{max_retries}{Passed to \code{\link[=perplexica_search_with_retry]{perplexica_search_with_retry()}}.}

\item{mode}{Optimisation mode passed to \code{\link[=perplexica_search]{perplexica_search()}}.}

\item{base_url}{Base URL of the Perplexica instance.}

\item{verbose}{If \code{TRUE}, prints each query and any retry attempts. Passed to
\code{\link[=perplexica_search_with_retry]{perplexica_search_with_retry()}}. Default \code{FALSE}.}
}
\value{
A data frame with columns \code{query} and \code{message}.
}
\description{
Runs \code{\link[=perplexica_search_with_retry]{perplexica_search_with_retry()}} for each element of \code{queries},
printing progress and saving periodic checkpoints so a crashed session can
be resumed.
}
\examples{
\dontrun{
queries <- c(
  "Current GDP of Brazil",
  "Population of Nigeria 2025",
  "Largest sovereign wealth funds by assets"
)
results <- perplexica_search_many(
  queries,
  checkpoint_file = "checkpoint.csv",
  checkpoint_every = 2
)

# Resume after a crash at row 2:
results <- perplexica_search_many(
  queries,
  checkpoint_file = "checkpoint.csv",
  resume_from = 2
)
}
}
